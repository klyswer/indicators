import { combineReducers } from 'redux';
import IndicatorInfo from '../reducers/indicators/IndicatorsReducer';

const rootReducer = combineReducers({
  IndicatorInfo,
});

export default rootReducer;
